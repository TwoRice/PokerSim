/**
* Genio (General Input, Output) class which takes user input as a string and converts to the
* required datatype displaying erorrs if input cannot be converted to the desired type and other
* data related methods
* 
*Author - Rory Magowan
* Version 1.0 23rd November 2015
*
*/
class Genio{

	public:

		static int readInt();
		static float readFloat();
		static double readDouble();
		static string strToLower(string str);

};